import { SerializedStyles } from "@emotion/react";

export interface SocialLoginIconProps {
  icon: string;
  className?: string;
  customStyle?: SerializedStyles;
}

export interface SocialLoginIconContainerProps {
  customStyle?: SerializedStyles;
}
